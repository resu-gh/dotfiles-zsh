### dotfiles - zsh

```console
rule help                           print help
rule build                          @combo [clean deps link]
rule deps                           install required dependencies
rule dirs                           create required dirs
rule link                           link zsh
rule clean                          clean/unlink zsh
```
